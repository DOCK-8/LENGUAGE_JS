version: '3.8'
services:
  # Configuración del servicio 1 : SERVIDOR
  server:
    build: .
    ports:
      - 8080:80
    volumes:
      - ./FE/:/usr/local/apache2/htdocs/

  # service2:
    # Configuración del servicio 2

# networks:
  # network1:
    # Configuración de la red 1

# volumes:
  # volume1:
    # Configuración del volumen 1
